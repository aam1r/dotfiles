[user]
    name = Aamir Mansoor
    email = me@aam1r.com
[github]
    user = aam1r
[alias]
    br = branch
    co = checkout
    st = status
    oneline = log --oneline
    patch = add --patch
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    sha1 = !sh -c 'git rev-parse ${1-`echo HEAD`}' -
[color]
    ui = auto
[color "branch"]
    current = green reverse
    local = green
    remote = cyan
[color "diff"]
    commit = magenta bold
    frag = cyan
    meta = bold
    new = green bold
    old = red bold
    whitespace = yellow reverse
[color "interactive"]
    prompt = green bold
    header = cyan
    help = yellow
    error = red bold
[color "status"]
    changed = yellow
[apply]
    whitespace = fix
[pager]
    status = true
    show-branch = true
[log]
    decorate = short
[core]
    excludesfile = /Users/aam1r/.global_ignore
[credential]
    helper = osxkeychain
